import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    card: {
        flexDirection: "row",
        alignItems: "flex-start",
        padding: 4,
    },
    title: {
        fontSize: 16,
        fontWeight: "600",
        color: "#000",
        marginBottom: 4,
    },
    content: {
        fontSize: 14,
        color: "#000",
        lineHeight: 20,
    },
});
